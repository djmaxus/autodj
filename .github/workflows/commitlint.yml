on:
  push:
    branches: ["master", "develop"]
  pull_request:
    branches: ["master", "develop"]
jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: opensource-nepal/commitlint@v1
